{
  "name": "fin29th",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "build": "clean-css-cli style.css -o style.min.css && terser script.js -o script.min.js && html-minifier index.html --collapse-whitespace --remove-comments --minify-css --minify-js -o index.min.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "html-minifier": "^4.0.0",
    "terser": "^5.44.1"
  }
}
